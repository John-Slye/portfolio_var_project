# Core data + math
yfinance>=0.2.40
pandas>=2.0
numpy>=1.24
scipy>=1.10
pyarrow>=14.0       # parquet engine used to cache downloaded prices

# Plotting
matplotlib>=3.7
seaborn>=0.12

# Volatility modeling (Phase 2: GARCH for Filtered Historical Simulation)
arch>=6.2

# Notebook
jupyter>=1.0
ipykernel>=6.0

# Phase 5 (optional, EVT). Comment out if you don't want it.
pyextremes>=2.3
